[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dataforge"
version = "0.1.0"
description = "DATAFORGE ANALYTICS internal utilities"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]